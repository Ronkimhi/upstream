__pycache__/
*.pyc
.DS_Store
.cache/
.pytest_cache/
trips.txt

# Stray bare-git clone left in the working tree (cloud add_repo artifact, 2026-08-29).
# Ignored rather than committed: a nested .git breaks clones. Ignored rather than deleted:
# Rule 19, removal is a move to Trash and that is Ron's call, not an agent's.
b647da95db3920c5e2ca69d39de5627766296bab/
