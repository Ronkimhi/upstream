# Deferred-work backlog

**Owned and groomed by Adam** (`.claude/agents/adam-gm.md`), from 2026-08-30. The within-mission
companion to `tasks/lessons.md`: lessons records what broke and earned a gate, this records what a
mission tripped over and deferred rather than chased.

**Why this exists.** A session on a mission that stops to fix every defect it finds turns one
mission into ten and finishes none. The rule in `CLAUDE.md` ("Mission focus and the deferred-work
backlog") sends every incidental, non-blocking finding here instead. Here it gets an id (its line),
a bucket, and a status, so it is visible the day it is found rather than buried in a `result:` field,
and so `run digest` can triage it. This replaces the old habit of writing "outside this build's
scope" into ledger prose, where a finding had no status and nobody owned the follow-up.

**How a row is written.** One line, appended, never rewritten except to advance its `status`:

`YYYY-MM-DD | found-under: <command or mission> | finding: <one line, concrete> | bucket: <concept-tag> | status: OPEN | note: <optional>`

- `found-under` is the mission the session was actually on when it tripped over this, so the
  interruption it would have cost is legible.
- `bucket` is a concept tag, not a per-item label. Two findings with the same root cause carry the
  same bucket, because the promotion works on buckets, not items: one concept, not one issue.
- `status` only advances, a row is never deleted: `OPEN` (captured, untriaged) then `BUCKETED`
  (grouped, waiting) then `PROMOTED` (a fix or gate is being built) then `DONE` or `WONTFIX`.

**How it is groomed.** `run digest` reads this file, groups OPEN rows by concept and root cause, and
promotes a bucket to a real fix or a gate only when it has earned it: the two-failure criterion in
`tasks/lessons.md`. Ten findings that share one cause become one structural fix, which is why the
small fixes never collide, they were never made separately. The digest `machine.deferred_backlog`
block reports the open count, the buckets, and the oldest OPEN row's age every week, so the backlog
can only go down or be seen not going down (Rule 21). Nothing is promoted on first sighting.

**No gate yet, on purpose.** This ships as a rule, a file, and a triage step, not a
`tools/check_*.py`. Hardening into a `tools/check_backlog.py` (rows well-formed, statuses monotonic,
append-only against git HEAD) or a derived `tools/backlog_board.py` view (mirroring
`tools/campaign_board.py`: read-only, `--write` emits one JSON, `app/build.py` inlines it, SCOPE
EMPTY over nothing) is a later dated decision, earned the first time this discipline fails twice: a
capture that should have happened and did not, or a promoted bucket that regressed. Building the gate
before then is the premature-enforcement mistake `check_machine.py` exists to avoid.

## Backlog

2026-08-29 | found-under: build agent 3 of 3 (Stocky, analyst plane) | finding: edgar_fts certifies an empty result (verified_zero) using the stooq price control probe, so a genuinely empty full-text search cannot be told apart from a dead EDGAR path whenever stooq is up | bucket: cross-plane-zero-verification | status: OPEN | note: ledger 2026-08-29 20:50Z; the insider path had the same shape and was fixed that day, the class fix is that each plane verifies its OWN zero, never stooq
2026-08-29 | found-under: build agent 3 of 3 (Stocky, analyst plane) | finding: companyfacts certifies its zero with the same stooq price probe, the same defect as edgar_fts, deferred with it as outside that build's scope | bucket: cross-plane-zero-verification | status: OPEN | note: ledger 2026-08-29 20:50Z; same bucket as the edgar_fts row above, so the promotion is one fix for both planes, not two
2026-08-30 | found-under: build the mission-focus deferred-work backlog | finding: a fresh app/build.py page is 2.63MB against build.py's own 2.0MB warn threshold, so tools/tests/test_campaign_ui.py::test_scale_stays_inside_single_html_budget fails; the single-HTML page inlines the full campaign UI, cortex 3D and all accumulated data | bucket: page-size-budget | status: OPEN | note: pre-existing and non-blocking, already 2.59MB at HEAD before this build and it is a warn threshold, the page still works; the class decision is trim the inlined payload or raise the threshold, a separate mission, captured here per this repo's own new rule rather than chased inline
2026-08-31 | found-under: draining click q-1788136315469 (run screen hormuz-maritime) | finding: three sessions independently claimed and analysed the same click within minutes, because the drain-claim convention used in cdb7206 exists in git but not in CLAUDE.md's click-queue protocol; item 5's duplicate rule ("skip it with a ledger NOTE") is unreachable because nothing tells a session where to look for an existing claim | bucket: click-queue-coordination | status: OPEN | note: two of the three sessions reached the identical correct conclusion and one wrote a partial screen file; the wasted work was the analysis, not the output. Cheapest fix is one sentence in the protocol naming the claim marker and requiring a git-log check before executing
2026-08-31 | found-under: run profile campaign (ai-infrastructure, depth-first) | finding: 12 listings in data/mappings/ai-infrastructure.json carry a bare local code with no exchange suffix (000660, 00100, 002156, 005930, 02513, 0700, 1810, 2308, 2330, 600584, 6239, 7011) and are not fetchable as written, so no market file, no fundamentals and no quality can ever be requested for them; four of them also appear a second time in suffixed form (ENR/ENR.DE, LT/LT.NS, SU/SU.PA, 7011/7011.T), so the same issuer is counted under two ticker spellings | bucket: listing-identity-normalization | status: BUCKETED | note: blocks nothing today because these names cannot reach O1 anyway under the method 6A canonical-metric rule, but it inflates every mapped-issuer denominator the campaign board reports and will mis-count coverage at completion UPDATE 2026-08-31: re-inspection found no duplicate listings (the four suffixed forms live in market_ticker on the same rows); the real gap is 12 rows with market_ticker null. Fix deferred to the next run universe ai-infrastructure amend+audit pair, because any material mapping change stales the PASS audit fingerprint and de-completes the campaign's only COMPLETE mapping until a fresh-context re-audit.
2026-08-31 | found-under: request data (ai-infrastructure profile batch) | finding: tools/fetch/fetch.py cik_for() line 175 does `base = ticker.split(".")[0].upper().replace("-","")` and matches that stub against SEC's US ticker table, so any suffixed foreign symbol silently adopts an unrelated US filer's CIK: data/market/ENR-DE.json (Siemens Energy, XETRA) carries cik 1632790 and SU-PA.json (Schneider Electric, Euronext) carries cik 311337, both US filers whose plain tickers are ENR and SU. do_fundamentals branches on that CIK, so a fundamentals fetch for either would write ANOTHER COMPANY's companyfacts into the foreign listing's file and quality would then compute Piotroski/Beneish/reverse-DCF from USD statements against a EUR price. Separately, a BARE mapped ticker that collides with a US symbol is worse still: the mapping's NXT is NEXTDC (ASX), and a bare NXT fetch would write a US NASDAQ filer to data/market/NXT.json, which then satisfies check_screen.py:372 (listing.ticker == row.ticker == row.market_ticker) and check_analyst identity exactly, so a wrong company could reach a verdict with NO gate firing | bucket: listing-identity-normalization | status: DONE | note: one-line fix is cik_for returning None when "." in ticker, so a suffixed symbol can never adopt a US filer's CIK; the bare-ticker collision needs the mapping re-spelled instead. CONTAINED not repaired: the 3 dangerous rows this session queued (NXT all kinds, ENR.DE + SU.PA fundamentals+quality) were removed while still PENDING, before the fetch ran. data/market/ is Actions-owned so the two stamped CIKs are not a session's to repair. No profile, screen row or dive may cite ENR-DE.json, SU-PA.json or NXT.json until this is fixed CLOSED 2026-08-31 (Path C repair mission): cik_for() no longer resolves any dotted ticker via the base-strip path (only the SEC class-share spelling BRK.B -> BRK-B may match), an explicit request-row `cik` field now asserts a real ADR link where one exists, and refresh rows REQ-20260831-183..197 were queued to rewrite the nine wrong-CIK market files (ENR.DE, SU.PA, NXT.AX, BA.L, AI.PA, ALV.DE, DIS.MI, MOH.AT, TOP.BK; AI.PA/ALV.DE/BA.L also carried the wrong company's fundamentals+quality). Tests: tools/tests/test_fetch_identity.py. The no-cite guard stands until those rows fulfill.
2026-08-31 | found-under: register routine upstream-campaign | finding: tools/check_analyst.py:711 selects the ledger lines it will police with a bare substring test, `[ln for ln in todays if "deepdive" in ln or "redteam" in ln]`, then takes dive_lines[-1] and demands verdict:/clock:/grade: in it. Any line dated today that merely MENTIONS those words in prose is therefore treated as a dive line: a NOTE registering the campaign routine, whose result: field described the funnel as "profile to screen to selection to deepdive to redteam", failed the gate even though a correct dive line existed earlier the same day. Two compounding causes: no word or field anchoring (the words are matched anywhere in the line, not in the COMMAND field), and taking only the LAST match rather than every dive line, so a later unrelated note masks a correct earlier one | bucket: unanchored-ledger-matchers | status: DONE | note: same defect class as the radar-gate and chain-gate unanchored path regexes fixed 2026-08-30, which is the SECOND failure for this bucket and by the two-failure criterion in tasks/lessons.md it has now earned a real fix rather than another workaround. Fix is to split the line on | and match the COMMAND field against ^(run )?(deepdive|redteam)\b, and to check every matching line rather than only the last. Worked around here by rewording the prose, which is exactly the kind of accommodation that should not be needed twice CLOSED 2026-08-31 (Path C repair mission): the matcher now splits on | and matches the COMMAND field against ^(run )?(deepdive|redteam)\b, checking every matching line rather than only the last; verified green over the very ledger day that broke it (the routine-registration NOTE no longer matches, the real MYRG dive line does).
2026-08-31 | found-under: run universe-audit munitions-replenishment (routine fire) | finding: link_coverage row `perchlorate-oxidizer` on data/mappings/munitions-replenishment.json carries status OPEN with zero ACTIVE placements and zero EXHAUSTED searches, i.e. the original `run universe` pass never touched it at all. tools/check_map.py's audit rule requires audit.sampled_checks to cover every link_coverage row (`checked_links == coverage_links`), and there is nothing on this link to sample, so no audit (PASS or FAIL) can ever validate for this mapping until the link gets at least one ACTIVE placement or a recorded EXHAUSTED search | bucket: universe-audit-blocked-by-untouched-link | status: DONE | note: a fresh-context universe-audit run confirmed this by trying (22 sampled rows covering 12/13 links, chain's own heat evidence already names AMPAC/NewMarket Corporation NYSE:NEU and Northrop Grumman as candidates for this exact link) and discarded its write rather than commit a failing gate state. Needs `run universe munitions-replenishment` targeted at perchlorate-oxidizer before `run universe-audit` can retry; campaign_board.py's worklist does not currently distinguish this from an audit-ready mapping, which is a second, smaller finding worth folding into the same fix. CLOSED 2026-08-31 18:22Z: both halves fixed in the same routine fire after a fourth consecutive stand-down on this exact wall. campaign_board.py now carries `_unauditable_links()` and refuses to recommend `run universe-audit` for a mapping with an untouched link_coverage row, recommending `run universe` instead (regression test tools/tests/test_board.py::test_mapping_with_an_untouched_link_recommends_universe_not_audit); the mapping itself gained one ACTIVE placement (Northrop Grumman on perchlorate-oxidizer) on evidence already cited by the chain, reused rather than re-fetched since WebFetch was EGRESS_BLOCKED again this run. NewMarket Corporation/AMPAC (NYSE: NEU) is still not a listed issuer here: that needs a fresh official identity_evidence fetch this session could not make, and is not re-opened as its own row since it was never blocking, only additive coverage
2026-08-31 | found-under: run universe-audit munitions-replenishment (routine fire) | finding: 4 Northrop Grumman ACTIVE placements on data/mappings/munitions-replenishment.json (energetic-materials, solid-rocket-motors, missile-final-assembly, artillery-and-bulk-munitions) cite only a stale FY2021 10-K against the mapping's own 2026-08-30 as_of date | bucket: stale-placement-evidence | status: OPEN | note: found during the same fresh-context audit attempt above; not blocking (audit could not run regardless), refresh to current filing when the mapping is next amended
2026-08-31 | found-under: run universe-audit munitions-replenishment (routine fire) | finding: this session's WebFetch egress policy blocked every fetch to the mapping's own cited evidence domains (LSE, Deutsche Boerse, Rheinmetall, Boeing, Leonardo, SGL Carbon, defensenews.com, sec.gov, q4cdn.com, elbitsystems.com, GPW, MISTRAS, VisiConsult), forcing WebSearch corroboration in place of direct document fetch for identity/role/source-date checks. This is the second time this exact failure mode has shown up in a routine ledger line (first at 2026-08-31 03:29Z, run profile --campaign, WebFetch to sec.gov/globenewswire/businesswire/prnewswire all EGRESS_BLOCKED) | bucket: webfetch-egress-blocked | status: PROMOTED | note: two occurrences now meets the two-failure bar in tasks/lessons.md; worth Adam's attention at the next run digest sweep rather than a session fix, since it is an environment/network-policy question, not a code defect PROMOTED 2026-08-31 by Ron's Path C decision: web-evidence stages run in local sessions; cloud routine fires do non-web work only and stand down honestly otherwise (CLAUDE.md venue note + docs/routines.md amended).
2026-08-31 | found-under: run radar (upstream-radar weekday routine) | finding: this session's WebFetch egress policy blocked every fetch attempted this run across 7 unrelated domains (noaa.gov, investors.metals.co, en.wikipedia.org, reuters.com, congress.gov, arxiv.org, oceanographicmagazine.com), all EGRESS_BLOCKED or unfetchable, with the agent proxy status itself healthy (0 recentRelayFailures) so the block sits above the proxy. This is the third occurrence of this exact failure mode (first at 2026-08-31 03:29Z run profile --campaign, second at run universe-audit munitions-replenishment) and blocked the radar mission outright: two candidate occurrences (deep-sea nodule mining commercialization via NOAA's 2026-01-21 rule and TMC's consolidated CCZ application; the IMO Net-Zero Framework's alternative marine-fuel bunkering buildout) were identified via WebSearch as strong, undercovered chain candidates but could not be written as signal cards because zero source pages could be opened for the verbatim source_excerpt the 2026-08-30 method section 1 rule requires | bucket: webfetch-egress-blocked | status: PROMOTED | note: third occurrence comfortably clears the two-failure bar; this is now a standing environmental block on the radar/campaign/universe-audit funnel stages alike, not stage-specific, and belongs at the top of Adam's next run digest sweep. The two candidate leads above are recorded here rather than lost so the next session with working WebFetch can pick them up directly: NOAA finalized its deep-seabed mining permit rule 2026-01-21 and TMC filed a consolidated exploration+commercial-recovery application covering ~65,000 km2 and 619Mt of nodules in the Clarion-Clipperton Zone, with a 60-day comment period ending 2026-10-19 and a targeted permit decision before end of Q1 2027; separately the IMO's Net-Zero Framework (approved MEPC 83, April 2025) is driving green methanol/ammonia bunkering infrastructure buildout (112+ methanol-capable ships already in commercial operation, Singapore issuing 2026-2030 methanol bunkering licenses) with WtW GHG conversion factors due Q2 2026 PROMOTED 2026-08-31 by Ron's Path C decision: web-evidence stages run in local sessions; cloud routine fires do non-web work only and stand down honestly otherwise (CLAUDE.md venue note + docs/routines.md amended).
2026-08-31 | found-under: run universe munitions-replenishment (routine fire, resolving the audit deadlock) | finding: WebFetch to sec.gov and newmarket.com both returned EGRESS_BLOCKED this run, the fourth occurrence of this exact failure mode today (03:29Z run profile --campaign, ~06:35Z run universe-audit munitions-replenishment, 11:54Z run radar, now) | bucket: webfetch-egress-blocked | status: PROMOTED | note: prevented adding NewMarket Corporation (NYSE: NEU, AMPAC's parent) as a listed issuer on perchlorate-oxidizer, since a brand-new listing needs fresh official identity_evidence this run could not fetch; closed the link instead with an already-cited Northrop Grumman placement. Fourth occurrence comfortably clears the two-failure bar already flagged twice in this bucket; still an environment/network-policy question for Adam's next run digest sweep, not a session-level code fix PROMOTED 2026-08-31 by Ron's Path C decision: web-evidence stages run in local sessions; cloud routine fires do non-web work only and stand down honestly otherwise (CLAUDE.md venue note + docs/routines.md amended).
2026-08-31 | found-under: artifact-republish notification, re-reading the click queue | finding: click q-1788136315469 (run screen hormuz-maritime) has now been read and re-analysed by at least seven sessions over 18 hours, producing seven "still BLOCKED" ledger lines and no progress, because the click-queue protocol has only two outcomes for an entry: EXECUTE it, or REJECT it as malformed. A valid command whose upstream stage has not run is neither, so it can never be drained and every session that reads the queue pays the same analysis again, forever | bucket: click-queue-coordination | status: OPEN | note: same bucket as the drain-claim row above, and this is its second and much more expensive failure, which is the two-failure criterion met. The missing concept is a third outcome, BLOCKED: drained from the queue once, recorded once with the prerequisite command named, and never re-analysed. Seven identical ledger lines in an append-only record is itself the damage: it trains a reader to skim the ledger
2026-08-31 | found-under: run universe-audit munitions-replenishment (routine fire) | finding: fifth occurrence today of the webfetch-egress-blocked failure mode, and the widest yet: 18/18 distinct domains failed this run, including neutral controls with no plausible content-policy reason (en.wikipedia.org, example.com, google.com) alongside every cited evidence domain. Checked the agent proxy status directly (`curl $HTTPS_PROXY/__agentproxy/status`): enabled, 0 recentRelayFailures, healthy. The block is confirmed above the proxy layer, consistent with the radar routine's same finding earlier today (backlog row 52) | bucket: webfetch-egress-blocked | status: PROMOTED | note: fifth occurrence; proxy-health check now rules out the one remaining "maybe it's the proxy" hypothesis, so this is squarely Adam's next run digest sweep item, and likely needs Ron's decision (org network policy / WebFetch tool config) rather than a code fix. The audit itself is otherwise sound: FAIL is honest and correctly denominated, not a workaround PROMOTED 2026-08-31 by Ron's Path C decision: web-evidence stages run in local sessions; cloud routine fires do non-web work only and stand down honestly otherwise (CLAUDE.md venue note + docs/routines.md amended).

## DONE 2026-08-31 · gate · `check_map` accepts an unfalsifiable `control_probe_passed`

CLOSED 2026-08-31 (Path C repair mission): `tools/check_map.py` now demands the `control_probe`
record whenever `control_probe_passed: true` is asserted, zero-hit or not, for searches dated
on or after 2026-09-01; the 56 pre-cutoff asserted-but-unprobed searches are counted in a
WARNING every run and can only be cleared by the next `run universe` pass on the affected map,
because a probe record cannot be honestly invented after the fact. Method §6A search-record text
and the Atlas contract amended to match. Refusal tests:
`tools/tests/test_map_proof.py::TestDatedGateHardenings`.

Found by the fresh-context audit of `munitions-replenishment`. All 10 EXHAUSTED searches assert `control_probe_passed: true`, but **no search on that map recorded zero hits**, so no `control_probe` record exists on any of them and the gate's control-probe requirement is never reached. The flag is currently an unverifiable self-assertion that costs an author nothing to write.

Method 6A demands a control probe precisely because a zero-hit search proves nothing on its own. Requiring the flag only when hits are zero means an author who never records a zero-hit search is never asked for one. Fix candidate: require `control_probe_passed` to be accompanied by a `control_probe` record whenever it is asserted at all, or drop the field on non-zero-hit searches so it cannot read as evidence.

## DONE 2026-08-31 · gate · exchange codes pass the excerpt test on substring luck

CLOSED 2026-08-31 (Path C repair mission): `exchange` and `ticker` now match the excerpt as
whole tokens for evidence dated on or after 2026-09-01 (`islse` can no longer satisfy `LSE`);
`legal_issuer` keeps the substring test. The 6 pre-cutoff substring-luck matches are counted
in the same WARNING line as the probe debt. Refusal tests:
`tools/tests/test_map_proof.py::TestDatedGateHardenings`.

Same audit. Six London listings carry `exchange: "LSE"`, and the excerpt test passes **only because the lowercase string `lse` happens to appear inside the venue code `MAINMARKET.SET1.LSEUK` and inside the field name `islse`** on the fetched page. The check is a substring match against the excerpt, so a short exchange code can be satisfied by an unrelated token. A two- or three-letter code is the common case, so this is not a one-off.

Fix candidate: match the exchange as a whole token, or against a closed list of exchange identifiers resolved from the listing's own venue field rather than free text.

## DONE 2026-08-31 · evidence · identity excerpts are composite, not contiguous

CLOSED 2026-08-31 by Ron's decision (Path C, recorded in method §1): for tabular sources the
excerpt may be assembled from the table's cells in reading order and must say so; every claimed
field must still appear in the fetched source. Assembly changes the shape of the quote, never
its contents.

Same audit, and it is honest about the severity: all 22 `identity_evidence` excerpts on that map are composite transcriptions rather than contiguous quotations. **Nothing is fabricated** (every claimed legal issuer, ticker and exchange does appear in the fetched source), but method section 1 asks for a verbatim span and a stitched one is not that. Exchange pages print these fields in table cells, so linearising them is the natural thing to do and will recur on every map.

Decision needed rather than a fix: either accept composite excerpts explicitly for tabular sources and say so in method section 1, or require one contiguous span per claimed field, which means three excerpts per listing instead of one.
2026-09-01 | found-under: attributing an artifact-republish notification with tools/queue_check.py | finding: the ledger's `artifact:` field has drifted well off the format CLAUDE.md specifies (`republished|skipped(<reason>)`). 27 lines use values it never defined, and 19 of those are INTENTIONS rather than outcomes: `pending` (7), `republish after push` (4), `republishing` (3), `PENDING` (2), plus `n/a` (8). An append-only ledger never comes back to say whether an intention happened, so those 19 lines record an artifact action whose result is permanently unknown | bucket: ledger-field-discipline | status: OPEN | note: found because tools/queue_check.py reads that field to decide whether a republish was ours; the drift makes it refuse attribution and fall back to a full page read, which is the safe direction, so this is cost rather than corruption. Cheapest fix is a validator over the ledger's own field vocabulary, the same shape validate.py already applies to every enum in data/
2026-09-01 | found-under: run profile --campaign CAMP-20260830-01 | finding: check_profile.py cannot see cross-issuer source contamination. Commit d6e6c4d landed five issuer profiles (AMKOR-TECHNOLOGY, MICRON-TECHNOLOGY, INTEL, TSMC, ASE-TECHNOLOGY) that were verbatim copies of data/companies/VERTIV-HOLDINGS.json: every canonical metric carried Vertiv's number sourced to data/market/VRT.json and companyfacts CIK 1674101, the business summary, exposure narrative, crowdedness caveats, catalysts with Vertiv's 8-K excerpts, data gaps and confidence audit were Vertiv's text, the changelog said it had created a profile for VERTIV-HOLDINGS, and placement role evidence pointed at the placeholder URL https://investor.example.com with the malformed date 2026-Q2. check_profile.py returned OK on all five, and so did validate.py | bucket: profile-gate-identity | status: OPEN | note: the gate validates that a numeric carries a source block, never that the source block belongs to this issuer. Three cheap checks would each have caught it independently: (a) every source url containing a CIK must match the profile's own listing cik, (b) a source_name naming a market store path must name this issuer's market file, (c) no url may be a reserved-for-documentation domain (example.com, example.org, localhost). (c) is a one-line denylist. Also worth adding: refuse a source_date that is not YYYY-MM-DD, which "2026-Q2" is not. Repaired by hand in this run; the gate is unchanged and would pass the same corruption again
2026-09-01 | found-under: run profile --campaign CAMP-20260830-01 | finding: SEC companyfacts returns zero annual statement concepts for IFRS foreign private issuers, so every 20-F filer on a chain is unprofilable. data/market/TSM.json and data/market/ASX.json both report coverage 0 of 20 annual fields with all twenty missing, and the fetcher's own verified_zero probe confirms the empty is real. TSMC filed a Form 20-F for fiscal 2025 and publishes full audited IFRS statements quarterly; the facts exist under the ifrs-full namespace and fetch.py reads only us-gaap | bucket: fetch-plane-taxonomy | status: OPEN | note: this blocks the single most important issuer on the ai-infrastructure chain, the leading-edge foundry choke point, and its direct OSAT comparable. Both profiles are BLOCKED with every canonical metric NULL as a result. No fetch request was queued for either because the control probe already proves a refetch returns nothing, so the request would pend forever. Fix is in fetch.py: read the ifrs-full namespace alongside us-gaap and map the IFRS concepts onto the same twenty annual fields
2026-09-01 | found-under: run profile --campaign CAMP-20260830-01 | finding: companyfacts coverage counts report 20 of 20 fields found while the series behind them are decades stale, so a profile reads as fully covered and then cannot compute anything. data/market/MU.json reports coverage 20/20 but long_term_debt ends at fiscal 2012-08-30, ppe_net at fiscal 2019-08-29 and total_debt carries one instant dated 2013-05-30, which leaves quality.piotroski and quality.beneish both PENDING_DATA on 0 common fiscal periods and NULLs net_debt_to_ebitda. data/market/INTC.json depreciation_fy ends at fiscal 2019-12-28 with a value of 200000000 that is inconsistent with Intel's scale | bucket: fetch-plane-coverage-honesty | status: OPEN | note: the coverage block counts whether a concept was ever found, not whether it was found for the latest fiscal year, so it is the wrong denominator to report. Fix candidate: report coverage against the latest fiscal period, and carry a per-field latest_period so a downstream reader can see the staleness without diffing the series. REQ-20260831-207 through -210 are queued for MU and INTC but may well return the same series, which is why this row exists
2026-09-01 | found-under: cortex UI work (Ron session) | finding: tools/tests/test_page_scale.py test_page_stays_inside_the_single_file_budget fails on main at HEAD fab8ca8, independent of any UI change: the projected finished-campaign page is 2,003,076 bytes against the 2,000,000 threshold, tipped over by data growth (heaviest projected stores: chains 419k, screens 213k, signals 203k). Verified by running the test with the UI diff stashed: it fails identically | bucket: page-size-projection | status: OPEN | note: the test's own message names the fix lane: raise a projection fidelity knob in app/build.py, never SIZE_WARN_MB. Belongs to whoever owns the projection knobs; a UI session declining to tune data-fidelity knobs mid-mission is the intended division
2026-09-01 | found-under: draining click q-1788224711345 (run radar), unfreezing the intake | finding: .github/workflows/fetch.yml's commit step recovers from exactly one conflicted path, app/index.html, so any rebase conflict on a data/ file the fetcher wrote leaves markers in the runner's checkout, fails validate, and discards the whole run including a successful feed batch. Observed on run 33457972947: 385 feed items fetched across 19 of 20 sources, then thrown away because a peer had pushed the same EDGAR documents and about 29 data/edgar/docs/*.json hit CONFLICT (add/add) | bucket: fetch-commit-conflict-recovery | status: OPEN | note: ESCALATED to Ron rather than applied, per nell-scanner.md line 236 (.github/workflows/ is never auto-applied). Both sides of such a conflict are the same fetcher writing the same public document, so "take either side" is correct and is already what CLAUDE.md prescribes for app/index.html. Second failure in this area today: the first was the refresh_dive_tickers KeyError, fixed in bb7c58b
2026-09-01 | found-under: build: the push queue (full-suite verification) | finding: tools/tests/test_page_scale.py::test_page_stays_inside_the_single_file_budget fails at clean origin/main, not only in dirty worktrees: the forward projection of the finished ten-theme campaign now exceeds app/build.py's 2.0 MB budget. The real page grew 1.53 MB to 1.7 MB today (more market files, screens, the board); the projection on top of that clears the threshold | bucket: page-size-budget | status: OPEN | note: the test's own message names the remedy: raise a projection fidelity knob in app/build.py, never SIZE_WARN_MB. The sessions that shipped elastic chain fidelity (08ff82f) own that knob and are active on exactly this; captured rather than chased so two sessions do not turn the same dial in opposite directions
2026-09-01 | found-under: contract audit (Adam, Ron session) | finding: uncommitted peer WIP on data/mappings/prediction-market-plumbing.json (+264 lines, partial census continuing 80440a5) makes tools/validate.py FAIL on this worktree (audit sampled links missing funding-and-settlement-rails, liquidity-provision), which blocks app/build.py for every concurrent session until the peer's postlude lands | bucket: peer-wip-blocks-shared-worktree | status: OPEN | note: not absorbed and not fixed here per mission focus and the never-touch-another-agent's-stores rule; this session skipped the page rebuild and recorded artifact: skipped. If the peer session died, the owning fix is Atlas resuming run universe prediction-market-plumbing
2026-09-01 | found-under: run profile --campaign CAMP-20260830-01 (tibet-mega-dam) | finding: a multi-chain issuer's COMPLETE profile only carries `placements[]` entries for the chain it was first profiled under, so campaign_calibrate.py/check_campaign.py's per-theme `completed_profiles` count silently undercounts every other chain that maps the same issuer. data/companies/GE-VERNOVA.json is COMPLETE/O2 and data/mappings/tibet-mega-dam.json carries 3 ACTIVE GE-VERNOVA placements (uhvdc-transmission, hydro-turbines, global-hydro-spillover), but the profile's own `placements[]` array holds only its 3 ai-infrastructure entries (energy-generation, power-equipment, grid-interconnect) from the batch that first wrote it; `_mapped_profile_placements` in check_campaign.py matches on the profile's own placements array against the canonical ACTIVE set, so tibet-mega-dam's `completed_profiles` reads 7 where 8 COMPLETE issuers actually resolve to a tibet-mega-dam placement | bucket: profile-cross-chain-placement-sync | status: OPEN | note: not fixed here (pre-existing, not created by this batch, and check_profile/check_campaign both pass without it since neither theme is near its completion targets yet). Fix candidate: `run profile --campaign` should append a mapped chain's placement block onto an existing profile it did not create, whenever a mapping shows an ACTIVE placement the profile's `placements[]` is missing, sourced from that mapping's own role evidence rather than re-deriving it
2026-09-01 | found-under: routine review (Ron session) | finding: the upstream-radar prompt in docs/routines.md presents its steps out of order: 1, 1b, 2, 2b, 2c, 3, 4, 5, 5c, 4b, 4c, 5b, 6, 7, 8. Steps 4b and 4c (answer every unclaimed surge, the magnitude test) sit AFTER 5 and 5c, and 5b (close the loop, run the calibrators and check_radar) sits after 4c. A fire that follows the prompt top to bottom runs the degraded-outcome and venue-standdown clauses before the surge answers the gate requires, and reaches the calibrators last only by accident of where they were appended | bucket: routine-prompt-ordering | status: OPEN | note: pure accretion damage, every clause was correct when appended and none was reordered. Not fixed in this pass because renumbering a live routine prompt changes what an unattended fire executes and deserves its own read-through against check_radar's actual required order rather than a cosmetic sort. The same accretion is visible but milder in upstream-campaign (13 clauses, venue split last). Fix candidate: renumber both prompts once, in execution order, and state at the top that the numbering IS the order
2026-09-01 | found-under: routine review (Ron session), pushing docs/routines.md | finding: SECOND failure in the peer-wip-blocks-shared-worktree bucket, and a wider class than the row above it recorded. Uncommitted peer WIP does not only fail validate.py and block app/build.py: it blocks `git pull --rebase` with "cannot pull with rebase: You have unstaged changes", which means tools/safe_push.py can never land ANY push from this worktree while the WIP sits there. Six push attempts across two safe_push runs were rejected and rebased with no progress, and safe_push reported "3 rounds did not land the push" without naming the unstaged-changes cause | bucket: peer-wip-blocks-shared-worktree | status: OPEN | note: worked around by landing the commit from a throwaway `git worktree add --detach` on origin/main, cherry-picking there and pushing, then `git reset --mixed origin/main` in the main worktree, which moves the branch pointer without touching the peer's file. That left 19 files showing as stale reversions (this worktree had never received the cloud routine's 0363a61) and they were restored from HEAD after verifying each was byte-identical to the old commit; the peer's 264-line audit block was confirmed intact afterwards. Two fix candidates, both cheap: (a) safe_push.py should detect unstaged changes before its first attempt and say so by name instead of burning three silent rounds, and (b) it could offer the detached-worktree path itself, since that is the only non-destructive way to push out of a worktree holding someone else's WIP
2026-09-02 | found-under: architecture review (Ron session, plan "from blockers to verdicts") | finding: the session's permission mode refuses every write under .claude/ (agents, skills, hooks), so three edits the review needs are written as patch files instead: tools/patches/universe-gate-placement.patch (the Stop hook does not yet recognise `run universe-audit <slug> --placement <issuer>`, so a placement-audit session is blocked at Stop until it lands) and tools/patches/contracts-2026-09-01.patch (sieve-profiler.md and the pull-data skill still describe the pre-decision vendor rule and do not state the dashed market filename) | bucket: claude-dir-writes-need-ron | status: DONE | note: Ron applies with `git apply <patch>`; check_model.py/check_machine.py will not notice the gap, so this row is the only record of it
2026-09-02 | found-under: architecture review (Ron session) | finding: 62 BLOCKED profiles carry a NULL basis claiming `data/market/<dotted>.json does not exist` while the dashed file sits on disk with fundamentals and a quality block (AFCONS.NS is the sample). check_profile.py now names each (WARN on pre-2026-09-02 profiles, FAIL once amended) and admits the vendor block into T2/T3 metrics, so the fix is four `run profile --campaign CAMP-20260901-01` batches, not a data request | bucket: profile-stale-path-claims | status: OPEN | note: the batches are the next command after this build lands; each profile amended today must clear its stale claim or fail the gate
2026-09-02 | found-under: architecture review (Ron session) | finding: THIRD failure in peer-wip-blocks-shared-worktree: the same uncommitted prediction-market-plumbing.json and candidates.json WIP makes validate.py, check_map.py and check_radar.py fail on this worktree while all three pass on HEAD in a detached worktree, so this build too lands via the detached-worktree route | bucket: peer-wip-blocks-shared-worktree | status: OPEN | note: the bucket is past the two-failure criterion; the promotion Adam owes is safe_push.py naming unstaged peer changes and offering the detached path itself
2026-09-02 | found-under: architecture review (Ron session), landing the build | finding: tools/safe_push.py cannot run from a linked git worktree: it opens `<root>/.git/upstream-push.lock` for writing, and in a worktree `.git` is a FILE pointing at the main repo's gitdir, so it dies with NotADirectoryError before taking the lock. The detached-worktree route is the only non-destructive way to push while peer WIP sits in the main worktree (rows above), so the push queue is unusable exactly when it is needed; this build pushed with a plain fast-forward `git push origin HEAD:main` from the worktree instead | bucket: peer-wip-blocks-shared-worktree | status: OPEN | note: fix is one line, resolve the lock path through `git rev-parse --git-common-dir`; fourth row in this bucket
2026-09-02 | found-under: architecture review (Ron session), landing run selection | finding: the `run selection` subagent moved the landing worktree's HEAD to origin/main with `git reset --mixed` before committing its own paths, which dropped the peer commit 1ddd607 (the tibet-mega-dam screen, its request rows and its ledger line) from history while leaving the files unstaged; recovered byte-identical in f5d99b7. SECOND failure of the destructive-git-verb class (tasks/lessons.md 2026-08-30 'Never git checkout --'), so it meets the two-failure criterion | bucket: destructive-git-verbs-in-shared-worktree | status: OPEN | note: promotion candidate is deterministic, which the lesson said it was not yet: a PreToolUse hook refusing `git reset`, `git checkout --`, `git rebase` and `git pull` from any subagent, leaving sync to the parent session or tools/safe_push.py
2026-09-02 | found-under: architecture review (Ron session), landing run selection and run deepdive | finding: two subagents re-dumped JSON stores at a different indentation while amending them (20 profiles turned ~25-line amendments into ~550-line diffs; the dive run caught itself doing the same to a 2,671-line screen), the a055a79 class again | bucket: json-reindent-on-amend | status: OPEN | note: fix candidate is one helper, tools/jsonio.py `dump_like(path, obj)` that detects the file's indentation and re-serialises with it, used by every writer; second sighting today, so promotion is due
2026-09-02 | found-under: run screen tibet-mega-dam | finding: the theme's only money-corner link (global-hydro-spillover) screens empty because 9 of its 10 ACTIVE placements are BLOCKED T3 profiles and the 10th, GE-VERNOVA, is COMPLETE but carries no tibet-mega-dam placement in its own profile (row 101's cross-chain sync gap, now blocking a screen row) | bucket: profile-cross-chain-placement-sync | status: OPEN | note: second consequence of the same cause; `run profile --campaign CAMP-20260901-01` on the tibet money-corner issuers is the next command, and the GE-VERNOVA placement amendment rides with it
2026-09-02 | found-under: run redteam 688425.SS tibet-mega-dam | finding: method section 7's earnings-quality ladder is non-monotonic: grade C caps the verdict at WATCH while grade D only forbids INVESTABLE, so a D book can carry TOO_LATE where a C book cannot; the first grade-C dive on disk (CRCHI, contract liabilities halved, DSO 314 days) made it visible | bucket: verdict-gate-ladder | status: OPEN | note: fix candidate is one sentence in section 7 (D caps at WATCH and forbids INVESTABLE; C caps at WATCH) plus the matching check in tools/check_analyst.py; a method change, so Ron's dated decision
2026-09-02 | found-under: run redteam 688425.SS tibet-mega-dam | finding: the chain's UNDISCOVERED rationale for tibet-mega-dam's tunneling-tbm link is contradicted by a fetched document: the beneficiary article that drove the +87% STAR basket already printed the 25-machine, ~CNY 6bn TBM sizing, so the link's crowdedness score was written before the market had read the same figure | bucket: stale-heat-evidence | status: OPEN | note: routed to Ember (`run heat tibet-mega-dam` re-scores the link with the article as dated evidence) and Atlas if the link's role text needs the parent-CRCC capture caveat; the two dives on the link stand, both WATCH
2026-09-02 | found-under: run redteam ANDR.VI tibet-mega-dam | finding: five consecutive dives have declined TOO_LATE and the shadow book has never graded a 'no', so the TOO_LATE hit-rate calibration method section 8 promises has no data yet | bucket: shadow-book-calibration | status: OPEN | note: not a defect in the dives (each declined on a measured decomposition); a scope boundary for `run review` to print beside its hit rate until the first TOO_LATE lands
2026-09-02 | found-under: run heat tibet-mega-dam | finding: a campaign theme whose chain has NO money-corner and NO UNDISCOVERED link is capped at stage SCENARIOS forever, and the two gates then contradict each other so no state on disk passes both. check_campaign.py:65 links_in_scope() under DEPTH returns only money_corner and UNDISCOVERED links; _actual_theme_stage (line 331) short-circuits on `not scope` and returns SCENARIOS before it can reach MAPPED, so an empty scope reads as nothing done rather than nothing owed. Today's heat re-score legitimately emptied tibet-mega-dam's scope (global-hydro-spillover lost its money corner, tunneling-tbm and india-counter-buildout lost UNDISCOVERED), and the theme dropped from COMPLETE to SCENARIOS while the chain still carries a COMPLETE mapping, 30 completed profiles, 2 O1 and 2 FINAL dives. The vice: leaving themes[5].stage at COMPLETE fails validate.py (`stage COMPLETE is ahead of evidence-backed stage SCENARIOS`), which refuses the build and therefore the commit; lowering it to SCENARIOS fails check_campaign.py:813 preservation (`stage regressed COMPLETE -> SCENARIOS`). The heat run left it at SCENARIOS because validate.py is the gate that refuses the build. | bucket: campaign-stage-scope-deadlock | status: OPEN | note: Ron's decision, two candidate fixes. (a) _actual_theme_stage treats an empty DEPTH scope as satisfied rather than as unmet, which is the reading the comment at line 326 actually describes. (b) preservation allows a stage regression when the recompute demands it and a changelog entry names the cause, which is what an evidence-derived stage means. Not fixed here: both are gate contracts outside Ember's lane, and one campaign field was corrected by hand only because the build was blocked.
2026-09-02 | found-under: run heat tibet-mega-dam | finding: tools/map_calibrate.py:65 band_for() disagrees with the canonical tools/heat_score.py band_for() at the CROWDED boundary: it uses `crowdedness > 60` where method section 3 and heat_score use `>= 60`, so every link scored at exactly 60 is reported as a verdict mismatch that check_heat and validate both accept. Visible today as `tibet-mega-dam (normalized): 1 verdict mismatch` for yajiang-developer (impact 95, crowdedness 60, written CROWDED, map_calibrate computes EMERGING). Pre-existing: the same string is in the committed _map-log.json at HEAD, and the chain changelog shows ember already corrected this exact link on 2026-08-30. The duplication is deliberate per the docstring at line 66 (the script must be able to report a disagreement the validator would reject), but a duplicate that has drifted reports a false disagreement forever, which trains the reader to skim the flag. | bucket: duplicated-derived-logic-drift | status: OPEN | note: fix candidate is one character, `>` to `>=` at map_calibrate.py:72, plus a test that the two band_for implementations agree on the four boundary values 40, 41, 60, 81. Not fixed here: a tools file outside Ember's lane and it blocks nothing.
2026-09-02 | found-under: run heat tibet-mega-dam (AMENDS the campaign-stage-scope-deadlock row above, same day) | finding: the deadlock in that row is pre-commit only and the row overstates it. check_campaign.py:813 preservation compares against git HEAD, so the SCENARIOS value failed only while it was uncommitted; after commit ca9df4b it is HEAD and check_campaign returns OK. What is real and stays open is narrower: an evidence-driven stage regression can never pass check_campaign before it is committed, so the postlude's only route is to commit the change the gate would have refused and let HEAD move under it, which makes the preservation rule unable to distinguish a corrected stage from a silently reverted one. | bucket: campaign-stage-scope-deadlock | status: OPEN | note: the empty-DEPTH-scope short circuit at check_campaign.py:331 is untouched by this correction and remains the primary question. Fix candidate (b) in the row above is unchanged: let preservation accept a regression whose manifest changelog names the recompute that demanded it, which would also let the gate see the difference between a corrected stage and a reverted one.
2026-09-02 | found-under: apply tools/patches (Ron session) | finding: the two patch files did not apply with `git apply` (hand-written hunks, wrong line counts); the changes were made directly with the Edit tool once the permission mode allowed .claude/ writes in this turn, so tools/patches/*.patch are now historical | bucket: claude-dir-writes-need-ron | status: DONE | note: universe-gate.py recognises `run universe-audit <slug> --placement <issuer>` and checks placement_audits[] for a same-day entry; sieve-profiler.md and pull-data SKILL.md carry the vendor-metric, dashed-filename, audit_scope and web_doc rules
2026-09-03 | found-under: would-buy zone gate (Ron, 2026-09-03) | finding: CLAUDE.md line 94 still says the Stop hook does not yet recognise the --placement form and that tools/patches/universe-gate-placement.patch carries the change for Ron to apply, but commit c0a6e70 applied it (.claude/hooks/universe-gate.py now dispatches universe-audit-placement) and the patch file is spent; a reader plans around a blocker that no longer exists | bucket: doc-drift | status: OPEN | note: one-line doc fix plus deleting or marking the spent patch; captured, not chased
2026-09-04 | found-under: run impact SIG-20260830-03 (click q-1788484798197) | finding: tools/impact_calibrate.py does not exclude PROMOTED candidates from unappraised_ids, so CAND-20260830-05 (the candidate SIG-20260830-03 was promoted from, appraised as IMP-20260830-06 and now re-pointed at the signal id) shows as unappraised although its occurrence is fully covered; a --queue run would re-appraise the same event, which the contract forbids | bucket: calibrator-identity | status: OPEN | note: captured by Tally, not chased; fix is one exclusion in the calibrator keyed on the candidate's promotion field
2026-09-04 | found-under: drain q-1788487857322 (run deepdive MTZ, click) | finding: validate FAILS at origin/main cc977a3 with 39 errors, all data/companies/CBL-INTERNATIONAL.json metric bases claiming data/market/BANL.json is missing after the fetch workflow landed it (workflow now lands fetches without a rebuild, per the 02:10Z note); every session on a clean checkout is refused by app/build.py until the profile is refreshed | bucket: stale-profile-after-fetch | status: OPEN | owner: Sieve (refresh data/companies/CBL-INTERNATIONAL.json)
| 2026-09-04 | page rebuild (carry everything) | 4 tests in tools/tests/test_stocky_admission.py (TestAdmissionMigrationBoundary: exact VRT path exemption, copied/renamed VRT, changed ticker/chain) FAIL at HEAD after VRT's identity backfill (commit 70e23b4, issuer_id VERTIV-HOLDINGS with no data/companies/VERTIV-HOLDINGS.json): the legacy-exemption fixture no longer matches the committed VRT bytes | analyst-gate-fixture | OPEN | not caused by the build change; either rebuild the fixture from the committed file or land the VERTIV-HOLDINGS profile |
2026-09-04 | found-under: run profile --campaign CAMP-20260901-01 (batch: money-corner/UNDISCOVERED/CHOKE_POINT priority) | finding: three BLOCKED profiles cannot be filled because their fetched vendor market files are genuinely empty (0/20 or PENDING_DATA coverage) despite FULFILLED request rows: HUNAN-WUXIN-INTELLIGENT-EQUIPMENT-GROUP (920174.BJ, Beijing Stock Exchange, likely unsupported venue class for yfinance), KONINKLIJKE-VOPAK (mapping market_ticker literally "VPK-AS" with a dash, not the Yahoo-format "VPK.AS" with a dot), SINOPEC-KANTONS-HOLDINGS (mapping market_ticker literally "0934-HK", not "0934.HK") | bucket: listing-identity-normalization | status: OPEN | note: no new PENDING request queued for these three this run (pull-data skill: "row would fulfill empty forever ... don't queue it"; all three already show FULFILLED against the same empty result). The two dash-suffixed tickers may be a genuine mapping-side spelling defect (Yahoo/yfinance tickers use a dot suffix, not a dash) rather than a real no-coverage result; owned by Atlas (data/mappings/hormuz-maritime.json), not Sieve. If corrected to VPK.AS / 0934.HK and refetched, these two may unblock; 920174.BJ (Beijing Stock Exchange) may be a genuine coverage gap independent of spelling.
| 2026-09-04 | request data (orchestrator) | data/companies/MARSH-MCLENNAN.json lists ticker MRSH (CIK 62709); Marsh & McLennan trades as MMC on NYSE, and data/market/MMC.json exists. The edgar_doc row for it carries the CIK so the fetch resolves, but the listing identity is wrong on the profile | listing-identity-normalization | OPEN | fix the listing with dated identity_evidence on the next run profile |
2026-09-04 | found-under: run universe-audit prediction-market-plumbing (fresh context, Ron 2026-09-04) | finding: SECOND occurrence, same shape. check_map.py's audit gate requires `audit.sampled_checks` to resolve every row in `link_coverage` (`checked_links == coverage_links`, unconditional on PASS or FAIL), but funding-and-settlement-rails and liquidity-provision carry zero ACTIVE placements and zero recorded searches, so no sampled_checks entry of any kind (placement, listing, or search) can ever resolve to them; a fresh-context audit that examines everything that exists on this map still tops out at 9/11 links_examined and check_map.py FAILs on `missing ['funding-and-settlement-rails', 'liquidity-provision']`. This is not a defect this session introduced: the 2026-09-01 01:25Z fresh-context audit (ledger line, `data/ledger.md:234`) hit the identical gap and named it verbatim in `amendments_required` ("an audit cannot attach a placement or EXHAUSTED-search sample to those two links. Close them as EXHAUSTED with a real multi-source census or add listed issuers"); that audit block itself was later stashed rather than committed. The 2026-09-04 19:10Z `run universe` correction (commit 24c6f85) read that exact finding (its own changelog calls it "Amendment 4") and explicitly declined to close it, on the grounds that both links are EMERGING/money_corner:false and out of DEPTH-mode scope. Verified today: re-fetched all 15 current ACTIVE placement sources and all 6 current EXHAUSTED-link search sources live; every one still matches its recorded excerpt, identity, and date, so the census itself has no defect this audit could act on other than the two unauditable rows. This session's audit could therefore not be committed at all: writing any schema-valid `audit` block into the mapping fails check_map.py on this one finding regardless of PASS/FAIL content, so the mapping was left untouched rather than repeat the 2026-09-01 orphaning. Two smaller findings surfaced in the same pass and would have gone into the FAIL audit's own `amendments_required`/`role_conflicts` had it landed: (a) the DRAFTKINGS placement on prediction-market-venue asserts the Railbird Exchange LLC acquisition as part of its claim but the placement's single source_excerpt only quotes the CFTC-oversight launch-date half; the second half is true and verbatim elsewhere in the same 10-K but not directly quoted. (b) sportsbook-platform-and-trading's TARGET_MET count of 5 is built from 4 vertically-integrated operators (DRAFTKINGS, RUSH-STREET-INTERACTIVE, BALLYS, CAESARS, each counted for running their OWN in-house platform under the role's "venue's own plumbing" language) plus 1 genuine third-party platform vendor (KAMBI); DRAFTKINGS is simultaneously counted on distribution-front-ends and prediction-market-venue, so "5 distinct issuers" is not 5 independent supply-side bets. | bucket: universe-audit-unexaminable-open-links | status: OPEN | note: two fix candidates, either closes the gap. (a) Author-side, cheapest: `run universe prediction-market-plumbing` closes funding-and-settlement-rails and liquidity-provision EXHAUSTED with a real 2-domain/2-source-type census, exactly the recipe already used 3 times on this same map (player-eligibility-gate, regulatory-gate, end-user-demand); once EXHAUSTED, their searches become sampleable and the audit can proceed. (b) Tooling-side: check_map.py's `audit_failures()` (the `checked_links != coverage_links` check) exempts an OPEN link carrying zero ACTIVE placements and zero recorded searches, since a fresh-context auditor cannot examine content that was never recorded; this is a dated Ron decision since tools/ is outside universe-audit's write scope. Past the two-failure criterion (tasks/lessons.md); ripe for promotion.
2026-09-04 | found-under: run deepdive POWL ai-infrastructure (Ron: full-machine look at Powell) | finding: every reverse DCF on disk multiplies a CURRENT price by a share count taken from the last annual filing, with no staleness check and no corporate-action awareness, so any split or material issuance between the two dates silently scales market cap and enterprise value by the split ratio. POWL is the live case: `quality.shares_used` is 12,070,178 as_of 2025-09-30 (pre-split) against a price of 182.71 as_of 2026-08-28, but Powell executed a three-for-one forward split trading split-adjusted from 2026-04-06 ("On April 2, 2026, Powell effected a three-for-one forward split of our common stock", verbatim from data/edgar/docs/POWL.json, the Q3 FY2026 8-K, and the same filing's diluted weighted-average share count is 36,604 thousand). So market_cap reads $2.205B against a true $6.688B, enterprise_value reads $1.572B against a true ~$6.054B, EV/FCF reads 10.2x against a true 39.1x, and `implied_fcf_cagr` reads -0.0734 where re-solving the file's own stated assumptions (9% discount, 2.5% terminal, bisection, base_fcf 154.788M) against the corrected EV gives +25.1% at 5y, +18.9% at 7y, +14.5% at 10y. The sign flips: the file says the market prices Powell for decline, the market actually prices it for 25% free-cash-flow compounding. The corrupt number then propagated: data/screens/ai-infrastructure.json buckets.pure_play[1] carries it tagged VERIFIED and its thesis_1line calls Powell "the only row here whose reverse DCF prices NEGATIVE forward FCF growth", which is how a screen surfaces a fake outlier to a dive. Blast radius measured this session: of 147 SOLVED reverse DCFs on disk, 118 use a share count more than 180 days older than the price it multiplies and 13 are more than 300 days stale, the worst being FCX (shares as_of 2011-12-31 against a 2026-08-28 price, 5,354 days), AON (2015-12-31, 3,893 days), JCI (2016-09-02, 3,647 days), MKL (2019-12-31, 2,436 days, and its -0.0751 is a negative outlier of exactly the shape that just proved false) and NXT (2023-07-03, 1,156 days, -0.133). Staleness is not by itself corruption, but nothing on disk distinguishes the two, and the defect is invisible precisely where it matters most: a name that split looks statistically cheap | bucket: stale-share-count-in-reverse-dcf | status: OPEN | note: found by two independent agents in the same session (a forensic read of the fundamentals and an arithmetic cross-check, 12,070,178 x 182.71 = 2,205,342,222.38 exactly, which is the market_cap on disk). Three fix candidates, none in this session's lane (tools/fetch is Actions-owned): (a) fetch.py takes the diluted weighted-average share count from the most recent 10-Q or 8-K rather than the last 10-K, which is the number that is already split-adjusted at source; (b) the quality block carries `shares_as_of` beside `price_as_of` and any reverse DCF whose gap exceeds one fiscal quarter is emitted as PENDING_DATA rather than SOLVED, so a stale solve cannot be read as a market signal; (c) a check_analyst.py and check_screen.py gate refusing any expectations-gap or thesis line that cites an implied growth rate whose two inputs are more than 180 days apart. (b) is the smallest change that makes the failure visible everywhere at once. Not chased here: Ron's mission was Powell, the POWL screen row is being corrected in-lane by the run selection agent, and REQ-20260904-40/41/42 were queued so Actions recomputes POWL's block from a post-split count
