# Deferred-work backlog

**Owned and groomed by Adam** (`.claude/agents/adam-gm.md`), from 2026-08-30. The within-mission
companion to `tasks/lessons.md`: lessons records what broke and earned a gate, this records what a
mission tripped over and deferred rather than chased.

**Why this exists.** A session on a mission that stops to fix every defect it finds turns one
mission into ten and finishes none. The rule in `CLAUDE.md` ("Mission focus and the deferred-work
backlog") sends every incidental, non-blocking finding here instead. Here it gets an id (its line),
a bucket, and a status, so it is visible the day it is found rather than buried in a `result:` field,
and so `run digest` can triage it. This replaces the old habit of writing "outside this build's
scope" into ledger prose, where a finding had no status and nobody owned the follow-up.

**How a row is written.** One line, appended, never rewritten except to advance its `status`:

`YYYY-MM-DD | found-under: <command or mission> | finding: <one line, concrete> | bucket: <concept-tag> | status: OPEN | note: <optional>`

- `found-under` is the mission the session was actually on when it tripped over this, so the
  interruption it would have cost is legible.
- `bucket` is a concept tag, not a per-item label. Two findings with the same root cause carry the
  same bucket, because the promotion works on buckets, not items: one concept, not one issue.
- `status` only advances, a row is never deleted: `OPEN` (captured, untriaged) then `BUCKETED`
  (grouped, waiting) then `PROMOTED` (a fix or gate is being built) then `DONE` or `WONTFIX`.

**How it is groomed.** `run digest` reads this file, groups OPEN rows by concept and root cause, and
promotes a bucket to a real fix or a gate only when it has earned it: the two-failure criterion in
`tasks/lessons.md`. Ten findings that share one cause become one structural fix, which is why the
small fixes never collide, they were never made separately. The digest `machine.deferred_backlog`
block reports the open count, the buckets, and the oldest OPEN row's age every week, so the backlog
can only go down or be seen not going down (Rule 21). Nothing is promoted on first sighting.

**No gate yet, on purpose.** This ships as a rule, a file, and a triage step, not a
`tools/check_*.py`. Hardening into a `tools/check_backlog.py` (rows well-formed, statuses monotonic,
append-only against git HEAD) or a derived `tools/backlog_board.py` view (mirroring
`tools/campaign_board.py`: read-only, `--write` emits one JSON, `app/build.py` inlines it, SCOPE
EMPTY over nothing) is a later dated decision, earned the first time this discipline fails twice: a
capture that should have happened and did not, or a promoted bucket that regressed. Building the gate
before then is the premature-enforcement mistake `check_machine.py` exists to avoid.

## Backlog

2026-08-29 | found-under: build agent 3 of 3 (Stocky, analyst plane) | finding: edgar_fts certifies an empty result (verified_zero) using the stooq price control probe, so a genuinely empty full-text search cannot be told apart from a dead EDGAR path whenever stooq is up | bucket: cross-plane-zero-verification | status: OPEN | note: ledger 2026-08-29 20:50Z; the insider path had the same shape and was fixed that day, the class fix is that each plane verifies its OWN zero, never stooq
2026-08-29 | found-under: build agent 3 of 3 (Stocky, analyst plane) | finding: companyfacts certifies its zero with the same stooq price probe, the same defect as edgar_fts, deferred with it as outside that build's scope | bucket: cross-plane-zero-verification | status: OPEN | note: ledger 2026-08-29 20:50Z; same bucket as the edgar_fts row above, so the promotion is one fix for both planes, not two
2026-08-30 | found-under: build the mission-focus deferred-work backlog | finding: a fresh app/build.py page is 2.63MB against build.py's own 2.0MB warn threshold, so tools/tests/test_campaign_ui.py::test_scale_stays_inside_single_html_budget fails; the single-HTML page inlines the full campaign UI, cortex 3D and all accumulated data | bucket: page-size-budget | status: OPEN | note: pre-existing and non-blocking, already 2.59MB at HEAD before this build and it is a warn threshold, the page still works; the class decision is trim the inlined payload or raise the threshold, a separate mission, captured here per this repo's own new rule rather than chased inline
2026-08-31 | found-under: draining click q-1788136315469 (run screen hormuz-maritime) | finding: three sessions independently claimed and analysed the same click within minutes, because the drain-claim convention used in cdb7206 exists in git but not in CLAUDE.md's click-queue protocol; item 5's duplicate rule ("skip it with a ledger NOTE") is unreachable because nothing tells a session where to look for an existing claim | bucket: click-queue-coordination | status: OPEN | note: two of the three sessions reached the identical correct conclusion and one wrote a partial screen file; the wasted work was the analysis, not the output. Cheapest fix is one sentence in the protocol naming the claim marker and requiring a git-log check before executing
2026-08-31 | found-under: run profile campaign (ai-infrastructure, depth-first) | finding: 12 listings in data/mappings/ai-infrastructure.json carry a bare local code with no exchange suffix (000660, 00100, 002156, 005930, 02513, 0700, 1810, 2308, 2330, 600584, 6239, 7011) and are not fetchable as written, so no market file, no fundamentals and no quality can ever be requested for them; four of them also appear a second time in suffixed form (ENR/ENR.DE, LT/LT.NS, SU/SU.PA, 7011/7011.T), so the same issuer is counted under two ticker spellings | bucket: listing-identity-normalization | status: OPEN | note: blocks nothing today because these names cannot reach O1 anyway under the method 6A canonical-metric rule, but it inflates every mapped-issuer denominator the campaign board reports and will mis-count coverage at completion
2026-08-31 | found-under: request data (ai-infrastructure profile batch) | finding: tools/fetch/fetch.py cik_for() line 175 does `base = ticker.split(".")[0].upper().replace("-","")` and matches that stub against SEC's US ticker table, so any suffixed foreign symbol silently adopts an unrelated US filer's CIK: data/market/ENR-DE.json (Siemens Energy, XETRA) carries cik 1632790 and SU-PA.json (Schneider Electric, Euronext) carries cik 311337, both US filers whose plain tickers are ENR and SU. do_fundamentals branches on that CIK, so a fundamentals fetch for either would write ANOTHER COMPANY's companyfacts into the foreign listing's file and quality would then compute Piotroski/Beneish/reverse-DCF from USD statements against a EUR price. Separately, a BARE mapped ticker that collides with a US symbol is worse still: the mapping's NXT is NEXTDC (ASX), and a bare NXT fetch would write a US NASDAQ filer to data/market/NXT.json, which then satisfies check_screen.py:372 (listing.ticker == row.ticker == row.market_ticker) and check_analyst identity exactly, so a wrong company could reach a verdict with NO gate firing | bucket: listing-identity-normalization | status: OPEN | note: one-line fix is cik_for returning None when "." in ticker, so a suffixed symbol can never adopt a US filer's CIK; the bare-ticker collision needs the mapping re-spelled instead. CONTAINED not repaired: the 3 dangerous rows this session queued (NXT all kinds, ENR.DE + SU.PA fundamentals+quality) were removed while still PENDING, before the fetch ran. data/market/ is Actions-owned so the two stamped CIKs are not a session's to repair. No profile, screen row or dive may cite ENR-DE.json, SU-PA.json or NXT.json until this is fixed
2026-08-31 | found-under: register routine upstream-campaign | finding: tools/check_analyst.py:711 selects the ledger lines it will police with a bare substring test, `[ln for ln in todays if "deepdive" in ln or "redteam" in ln]`, then takes dive_lines[-1] and demands verdict:/clock:/grade: in it. Any line dated today that merely MENTIONS those words in prose is therefore treated as a dive line: a NOTE registering the campaign routine, whose result: field described the funnel as "profile to screen to selection to deepdive to redteam", failed the gate even though a correct dive line existed earlier the same day. Two compounding causes: no word or field anchoring (the words are matched anywhere in the line, not in the COMMAND field), and taking only the LAST match rather than every dive line, so a later unrelated note masks a correct earlier one | bucket: unanchored-ledger-matchers | status: OPEN | note: same defect class as the radar-gate and chain-gate unanchored path regexes fixed 2026-08-30, which is the SECOND failure for this bucket and by the two-failure criterion in tasks/lessons.md it has now earned a real fix rather than another workaround. Fix is to split the line on | and match the COMMAND field against ^(run )?(deepdive|redteam)\b, and to check every matching line rather than only the last. Worked around here by rewording the prose, which is exactly the kind of accommodation that should not be needed twice
2026-08-31 | found-under: run universe-audit munitions-replenishment (routine fire) | finding: link_coverage row `perchlorate-oxidizer` on data/mappings/munitions-replenishment.json carries status OPEN with zero ACTIVE placements and zero EXHAUSTED searches, i.e. the original `run universe` pass never touched it at all. tools/check_map.py's audit rule requires audit.sampled_checks to cover every link_coverage row (`checked_links == coverage_links`), and there is nothing on this link to sample, so no audit (PASS or FAIL) can ever validate for this mapping until the link gets at least one ACTIVE placement or a recorded EXHAUSTED search | bucket: universe-audit-blocked-by-untouched-link | status: OPEN | note: a fresh-context universe-audit run confirmed this by trying (22 sampled rows covering 12/13 links, chain's own heat evidence already names AMPAC/NewMarket Corporation NYSE:NEU and Northrop Grumman as candidates for this exact link) and discarded its write rather than commit a failing gate state. Needs `run universe munitions-replenishment` targeted at perchlorate-oxidizer before `run universe-audit` can retry; campaign_board.py's worklist does not currently distinguish this from an audit-ready mapping, which is a second, smaller finding worth folding into the same fix
2026-08-31 | found-under: run universe-audit munitions-replenishment (routine fire) | finding: 4 Northrop Grumman ACTIVE placements on data/mappings/munitions-replenishment.json (energetic-materials, solid-rocket-motors, missile-final-assembly, artillery-and-bulk-munitions) cite only a stale FY2021 10-K against the mapping's own 2026-08-30 as_of date | bucket: stale-placement-evidence | status: OPEN | note: found during the same fresh-context audit attempt above; not blocking (audit could not run regardless), refresh to current filing when the mapping is next amended
2026-08-31 | found-under: run universe-audit munitions-replenishment (routine fire) | finding: this session's WebFetch egress policy blocked every fetch to the mapping's own cited evidence domains (LSE, Deutsche Boerse, Rheinmetall, Boeing, Leonardo, SGL Carbon, defensenews.com, sec.gov, q4cdn.com, elbitsystems.com, GPW, MISTRAS, VisiConsult), forcing WebSearch corroboration in place of direct document fetch for identity/role/source-date checks. This is the second time this exact failure mode has shown up in a routine ledger line (first at 2026-08-31 03:29Z, run profile --campaign, WebFetch to sec.gov/globenewswire/businesswire/prnewswire all EGRESS_BLOCKED) | bucket: webfetch-egress-blocked | status: OPEN | note: two occurrences now meets the two-failure bar in tasks/lessons.md; worth Adam's attention at the next run digest sweep rather than a session fix, since it is an environment/network-policy question, not a code defect
2026-08-31 | found-under: run radar (upstream-radar weekday routine) | finding: this session's WebFetch egress policy blocked every fetch attempted this run across 7 unrelated domains (noaa.gov, investors.metals.co, en.wikipedia.org, reuters.com, congress.gov, arxiv.org, oceanographicmagazine.com), all EGRESS_BLOCKED or unfetchable, with the agent proxy status itself healthy (0 recentRelayFailures) so the block sits above the proxy. This is the third occurrence of this exact failure mode (first at 2026-08-31 03:29Z run profile --campaign, second at run universe-audit munitions-replenishment) and blocked the radar mission outright: two candidate occurrences (deep-sea nodule mining commercialization via NOAA's 2026-01-21 rule and TMC's consolidated CCZ application; the IMO Net-Zero Framework's alternative marine-fuel bunkering buildout) were identified via WebSearch as strong, undercovered chain candidates but could not be written as signal cards because zero source pages could be opened for the verbatim source_excerpt the 2026-08-30 method section 1 rule requires | bucket: webfetch-egress-blocked | status: OPEN | note: third occurrence comfortably clears the two-failure bar; this is now a standing environmental block on the radar/campaign/universe-audit funnel stages alike, not stage-specific, and belongs at the top of Adam's next run digest sweep. The two candidate leads above are recorded here rather than lost so the next session with working WebFetch can pick them up directly: NOAA finalized its deep-seabed mining permit rule 2026-01-21 and TMC filed a consolidated exploration+commercial-recovery application covering ~65,000 km2 and 619Mt of nodules in the Clarion-Clipperton Zone, with a 60-day comment period ending 2026-10-19 and a targeted permit decision before end of Q1 2027; separately the IMO's Net-Zero Framework (approved MEPC 83, April 2025) is driving green methanol/ammonia bunkering infrastructure buildout (112+ methanol-capable ships already in commercial operation, Singapore issuing 2026-2030 methanol bunkering licenses) with WtW GHG conversion factors due Q2 2026
