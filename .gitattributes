# Merge semantics the whole machine shares, applied by git itself in every venue,
# including the Actions runner. The ledger is append-only and its conflict rule is
# UNION, never choose (CLAUDE.md postlude step 5): both sides record something that
# actually happened. `merge=union` is built into git and needs no per-clone setup.
data/ledger.md merge=union
